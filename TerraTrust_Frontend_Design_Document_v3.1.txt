================================================================================
FRONTEND DESIGN DOCUMENT (FDD)
PROJECT: TERRATRUST-AR
================================================================================

Version       : 3.1 (Maps strategy updated + OCR reference corrected)
Platform      : Android (Primary), iOS (Future Scope)
Framework     : React Native CLI (0.73+)
Language      : TypeScript (Strict Mode)
Last Updated  : March 2026

--------------------------------------------------------------------------------
WHAT CHANGED FROM PREVIOUS VERSION (v3.0)
--------------------------------------------------------------------------------

8. Maps strategy clarified — split into two sources (no single map provider
   for everything).
   Previous design: react-native-maps with Google Maps satellite view used for
   all map screens.
   Updated design:
     - ZoneNavigationScreen: Google Maps via react-native-maps. Used for farmer
       GPS navigation to sampling zones. Road map view with overlays. Stays
       within Google Maps free tier (10,000 loads/month) at MVP scale.
     - BoundaryConfirmScreen: Satellite imagery sourced from Google Earth Engine
       (Sentinel-2 true colour composite). Backend generates a static PNG of
       the farm area using GEE (already in the project for biomass work) and
       returns it to the app. Displayed as a background image layer in
       react-native-maps with mapType="none". Polygon overlay drawn on top.
       This removes all dependency on Google Maps satellite tiles which cost
       money and replaces them with free Sentinel-2 imagery that is often more
       current and clearer for agricultural land.
     - LandListScreen thumbnails: Static satellite PNG per land parcel, also
       generated by GEE backend. Cached after first fetch.
   Net result: react-native-maps library stays. Google Maps API key still
   required (for ZoneNavigationScreen). Zero additional cost.

9. OCR server description corrected.
   Section 12 (Offline Strategy) previously stated "Document OCR requires
   backend Tesseract server." Updated to reflect that the backend now uses
   PaddleOCR PP-OCRv5 (changed in backend document v3.1). The frontend
   behaviour is identical — OCR still happens on the server, farmer still
   sees the same extracted fields. Only the server-side engine changed.

--------------------------------------------------------------------------------
WHAT CHANGED FROM VERSION v2.1 (carried over from v3.0)
--------------------------------------------------------------------------------

The following changes were made in v3.0 and remain in effect:

1. Firebase replaced by Supabase for authentication.
   Reason: Supabase provides PostgreSQL + PostGIS + Auth + File Storage in one
   free platform. Firebase was a separate service that added complexity.
   Impact: Replace firebase/auth calls with @supabase/supabase-js. The farmer-
   facing OTP login flow remains identical.

2. Land boundary fetching is now automated (3-layer system).
   Previous design had two modes: Draw Mode (manual polygon tap) and Walk Mode
   (GPS-based). Both are now REMOVED as primary methods.
   Reason: Official government land boundary must come from government records,
   not farmer-drawn polygons, to be accepted by carbon markets.
   New approach: OCR the 7/12 document → auto-fetch official boundary via
   BhuNaksha WMS API → farmer only confirms what is shown.
   Manual drawing retained only as last-resort fallback if all API layers fail.

3. AR Measurement System changed from single-tier to 3-tier.
   Previous design assumed all phones support ARCore Depth API.
   Reality: Not all Android phones have depth sensor hardware.
   New approach: Tier 1 (full depth AR), Tier 2 (motion-based AR), Tier 3
   (manual string measurement). App auto-detects which tier applies.

4. AR no longer measures tree height as primary source.
   Previous design: AR measures height using AR anchors (user taps base, tilts
   to top).
   New design: Height comes from NASA GEDI satellite data where available.
   AR height measurement retained as fallback only when GEDI has no data for
   that location.
   Reason: GEDI provides more consistent height measurements across the whole
   farm without farmer error.

5. Sampling zones are now satellite-guided, not arbitrary.
   Previous design: 3-5 zones placed based on compass directions (North, South,
   Center).
   New design: Backend runs Sentinel-2 NDVI analysis on the farm first. Zones
   are placed inside distinct vegetation density areas identified by satellite.
   Farmer follows a linear walking path (like Google Maps navigation), not
   random directions.

6. Redux persist storage changed from AsyncStorage to MMKV.
   AsyncStorage is slow and not encrypted. MMKV is 30x faster, synchronous,
   and secure. Critical for AR scan data that must save instantly.

7. react-native-image-picker replaced by react-native-vision-camera for
   document scanning.
   Reason: Higher quality capture needed for OCR accuracy on 7/12 documents.

================================================================================
SECTION 1: ARCHITECTURE OVERVIEW
================================================================================

1.1 DESIGN PATTERN
------------------
The application follows two principles:

Atomic Design Methodology: Components are broken into Atoms (Button, Text,
Icon), Molecules (InputField, OTPBox), Organisms (LoginForm, TreeScanCard),
Templates, and Screens. This ensures reuse and consistency.

Feature-First Folder Structure: All code for a feature lives together.
For example, src/features/ar-audit/ contains the screens, Redux slice, and
utilities for the AR audit module. This scales better than grouping by file
type.

1.2 TECHNOLOGY STACK
--------------------

Component             | Technology                  | Version  | Reason
----------------------|-----------------------------|---------|--------------------------
Core Framework        | React Native CLI            | 0.73+   | Required for Native ARCore
                      |                             |         | bridge. Expo cannot access
                      |                             |         | ARCore Depth API directly.
Language              | TypeScript                  | 5.0+    | Type safety for complex AR
                      |                             | (Strict)| data and GeoJSON objects.
State Management      | Redux Toolkit (RTK)         | 2.0+    | Manages global state for
                      |                             |         | User, Land, Audit, Credits.
Navigation            | React Navigation            | v6      | Native stack navigator.
Maps                  | react-native-maps           | Latest  | Used for ZoneNavigation-
                      |                             |         | Screen (Google Maps road
                      |                             |         | map with GPS overlays).
                      |                             |         | Also used as the base
                      |                             |         | container in Boundary-
                      |                             |         | ConfirmScreen with
                      |                             |         | mapType="none" and GEE
                      |                             |         | satellite image as layer.
Satellite Imagery     | Google Earth Engine (GEE)   | Backend | Sentinel-2 true colour
                      | — backend generated PNG     |         | satellite image for
                      |                             |         | BoundaryConfirmScreen
                      |                             |         | and land thumbnails.
                      |                             |         | Free, no tile limits.
                      |                             |         | Backend returns static
                      |                             |         | PNG URL to the app.
AR Bridge             | Native Modules (Kotlin)     | Custom  | Direct ARCore Depth API.
                      |                             |         | No JS library can do this.
Camera                | react-native-vision-camera  | v4      | High-quality document and
                      |                             |         | AR frame capture.
Form Validation       | Zod + React Hook Form       | Latest  | Type-safe form validation.
Styling               | NativeWind (Tailwind)       | 4.0+    | Utility class styling.
Local Storage         | MMKV                        | Latest  | Fast, synchronous, encrypted
                      |                             |         | key-value storage.
Offline Persistence   | Redux Persist               | Latest  | Survives app crashes.
Location              | react-native-geolocation-   | Latest  | GPS for zone navigation and
                      | service                     |         | geofencing.
HTTP Client           | Axios                       | Latest  | API calls with interceptors.
Animations            | Lottie + Reanimated 2       | Latest  | Loading states, AR overlays.
Blockchain (Wallet)   | ethers.js                   | v6      | Farmer wallet generation and
                      |                             |         | token balance reading.
Authentication        | @supabase/supabase-js       | Latest  | Phone OTP authentication.
                      |                             |         | Replaces Firebase.
Species Detection     | TensorFlow Lite             | Latest  | On-device tree species
                      |                             |         | identification. No internet
                      |                             |         | required.
Background Sync       | react-native-background-    | Latest  | Auto-uploads pending audits
                      | fetch                       |         | when internet returns.
Crypto Hashing        | react-native-quick-crypto   | Latest  | SHA-256 hashing of evidence
                      |                             |         | photos on-device before upload.
Device Security       | react-native-device-info    | Latest  | Mock GPS detection + rooted
                      |                             |         | device warning.
Haptics               | react-native-haptic-        | Latest  | Vibration feedback on
                      | feedback                    |         | successful tree scan.
Config / Env          | react-native-config         | Latest  | Loads .env variables into app.
Animations            | lottie-react-native         | Latest  | Loading states (spinning leaf),
                      |                             |         | scan success, token mint.
Motion Animations     | react-native-reanimated     | 2+      | AR overlay transitions and
                      |                             |         | smooth screen animations.
Charting              | react-native-chart-kit      | Latest  | Bar chart on
                      | + react-native-svg          |         | CreditHistoryScreen.

================================================================================
SECTION 2: FOLDER STRUCTURE
================================================================================

TerraTrustAR/
├── android/
│   ├── app/
│   │   ├── src/main/
│   │   │   ├── java/com/terratrustar/
│   │   │   │   ├── ar/
│   │   │   │   │   ├── ARModule.kt      # ARCore depth + RANSAC cylinder fitting
│   │   │   │   │   └── ARPackage.kt     # Registers ARModule with React Native
│   │   │   │   └── MainApplication.kt  # Register ARPackage here
│   │   │   └── AndroidManifest.xml
│   │   ├── build.gradle                # local.properties + ARCore + TFLite deps
│   │   └── google-services.json        # From Google Cloud Console
│   └── local.properties                # GOOGLE_MAPS_API_KEY (never commit)
│
├── src/
│   ├── app/
│   │   └── App.tsx                     # Root component + navigation setup
│   │
│   ├── assets/
│   │   ├── fonts/                      # Roboto font family files
│   │   ├── images/                     # Icons, logos, tutorial images
│   │   ├── lottie/                     # Lottie JSON animation files
│   │   │   ├── spinning_leaf.json      # Loading animation (OCR, satellite)
│   │   │   ├── scan_success.json       # Tree scan success animation
│   │   │   └── credit_earned.json      # Token minting celebration
│   │   └── tflite/
│   │       └── species_model.tflite    # On-device tree species model
│   │
│   ├── features/
│   │   ├── auth/
│   │   │   ├── screens/
│   │   │   │   ├── SplashScreen.tsx    # Session check → route to Login or Home
│   │   │   │   ├── LoginScreen.tsx
│   │   │   │   ├── OTPScreen.tsx
│   │   │   │   └── KYCScreen.tsx
│   │   │   └── store/
│   │   │       └── authSlice.ts        # Redux slice for auth state
│   │   │
│   │   ├── land/
│   │   │   ├── screens/
│   │   │   │   ├── LandListScreen.tsx
│   │   │   │   ├── DocumentUploadScreen.tsx
│   │   │   │   ├── BoundaryConfirmScreen.tsx
│   │   │   │   └── ManualUploadGuideScreen.tsx   # Layer 3 fallback guide
│   │   │   └── store/
│   │   │       └── landSlice.ts
│   │   │
│   │   ├── ar-audit/
│   │   │   ├── screens/
│   │   │   │   ├── AuditStartScreen.tsx
│   │   │   │   ├── ZoneNavigationScreen.tsx  # Google Maps road map
│   │   │   │   ├── ARCameraScreen.tsx
│   │   │   │   ├── ManualMeasureScreen.tsx   # Tier 3 string measurement input
│   │   │   │   ├── TreeResultScreen.tsx
│   │   │   │   └── AuditCompleteScreen.tsx
│   │   │   └── store/
│   │   │       └── auditSlice.ts
│   │   │
│   │   └── dashboard/
│   │       ├── screens/
│   │       │   ├── HomeScreen.tsx
│   │       │   └── CreditHistoryScreen.tsx
│   │       └── store/
│   │           └── creditsSlice.ts
│   │
│   ├── services/
│   │   ├── api.ts              # Axios config + JWT interceptor
│   │   ├── ar-bridge.ts        # TypeScript interface to ARModule.kt
│   │   ├── wallet.ts           # ethers.js wallet creation
│   │   ├── blockchain.ts       # Token balance read from Polygon
│   │   └── supabase.ts         # Supabase client initialisation
│   │
│   ├── common/
│   │   ├── components/         # Shared UI atoms and molecules
│   │   │   ├── Button.tsx      # Primary, Secondary, Destructive variants
│   │   │   ├── Card.tsx
│   │   │   ├── Badge.tsx       # Verified, Pending, Error status badges
│   │   │   ├── BottomSheet.tsx # Slide-up panel for scan results
│   │   │   └── Loader.tsx      # Lottie spinning leaf wrapper
│   │   ├── hooks/
│   │   │   ├── useGeofence.ts  # Real-time GPS vs land boundary check
│   │   │   └── useARTier.ts    # Detect AR capability on startup
│   │   ├── utils/
│   │   │   ├── geoJson.ts      # Parse and validate GeoJSON polygons
│   │   │   ├── units.ts        # Circumference to diameter, acres to ha
│   │   │   └── hash.ts         # SHA-256 hashing for evidence photos
│   │   └── constants/
│   │       ├── colors.ts       # Full colour palette (see Section 9)
│   │       └── species.ts      # Species names + wood density table
│   │
│   └── store/
│       ├── index.ts            # Redux store setup + redux-persist config
│       └── mmkvStorage.ts      # MMKV adapter for redux-persist
│
├── .env.development             # Development environment variables
├── .env.production              # Production environment variables
└── .gitignore                   # Must include: .env*, local.properties

================================================================================
SECTION 3: MODULE 1 — AUTHENTICATION
================================================================================

3.1 OVERVIEW
------------
Authentication uses Supabase phone OTP. The farmer enters their Indian mobile
number, receives a 6-digit SMS code, and is logged in. No passwords. No email.
This is the only way any Indian farmer should be expected to log in.

KYC (Aadhaar + Name) is collected once at first login. The name collected here
is later cross-checked against the owner name extracted from their 7/12 land
document by OCR. They must match exactly, otherwise land registration is
rejected.

When a new farmer logs in for the first time and has no wallet address on
record, the app silently generates a blockchain wallet. The farmer sees nothing
of this process. It happens in the background. This wallet receives their
Carbon Ton Tokens when they earn them.

3.2 SCREENS
-----------

SCREEN: SplashScreen
  What farmer sees:  App logo centred on screen. Lottie loading indicator.
  Logic:             Check if valid Supabase auth session exists in MMKV.
                     If session valid and KYC complete → navigate to HomeScreen.
                     If session valid but KYC not done → navigate to KYCScreen.
                     If no valid session → navigate to LoginScreen.

SCREEN: LoginScreen
  What farmer sees:  Single phone number input field with +91 prefix shown.
                     One button: "Send OTP"
  Validation:        Number must be exactly 10 digits after the prefix.
  On submit:         Call supabase.auth.signInWithOtp({ phone: '+91XXXXXXXXXX' })
  On success:        Navigate to OTPScreen

SCREEN: OTPScreen
  What farmer sees:  Six individual digit boxes. Instruction: "Enter the 6-digit
                     code sent to your number"
                     Timer showing "Resend in 28s" counting down to zero.
                     After timer hits zero: "Resend OTP" link appears.
  Behaviour:         Each box auto-focuses the next box when a digit is entered.
                     Backspace moves focus back to previous box.
  On verify:         Call supabase.auth.verifyOtp({ phone, token, type: 'sms' })
  On success:        Check if wallet_address exists in user profile.
                     If wallet_address is null: run silent wallet creation (see
                     Section 3.3 below), then check if KYC is complete.
                     If KYC not done: navigate to KYCScreen.
                     If KYC done: navigate to HomeScreen.

SCREEN: KYCScreen
  What farmer sees:  Two input fields: Full Name, Aadhaar Number (12 digits).
                     Instruction text: "Enter your name exactly as written on
                     your land document (7/12 Extract)."
                     One button: "Continue"
  Why this matters:  The name entered here is matched against the OCR-extracted
                     owner name from the land document in Module 2. Mismatch
                     blocks land registration.
  On submit:         POST /api/v1/auth/kyc to backend. Backend hashes Aadhaar
                     with SHA-256 (never stored plain text). Stored in Supabase
                     users table. Navigate to HomeScreen.

3.3 SILENT WALLET CREATION
---------------------------
Happens automatically after first OTP verification when no wallet exists.
The farmer never sees this process and never interacts with it.

Step 1: ethers.js generates a random private key and derives the public wallet
        address from it.
Step 2: The private key is encrypted and stored in react-native-keychain, which
        uses the phone's hardware-backed secure storage (Android Keystore).
        The key never leaves the device. It is never stored in Redux state.
        It is never sent to any server.
Step 3: Only the public wallet address is sent to the Supabase backend and
        stored in the users table under wallet_address.
Step 4: When the farmer earns Carbon Ton Tokens, the backend mints them to this
        wallet address. The farmer sees their balance in the Dashboard as a
        plain number, not as a blockchain address.

Code reference:
  // services/wallet.ts
  import { ethers } from 'ethers';
  import * as Keychain from 'react-native-keychain';

  export const createFarmerWallet = async (): Promise<string> => {
    const wallet = ethers.Wallet.createRandom();
    await Keychain.setGenericPassword(
      'wallet_private_key',
      wallet.privateKey,
      { accessControl: Keychain.ACCESS_CONTROL.BIOMETRY_ANY_OR_DEVICE_PASSCODE }
    );
    return wallet.address; // Only the public address is returned
  };

================================================================================
SECTION 4: MODULE 2 — LAND VERIFICATION
================================================================================

4.1 OVERVIEW
------------
This module verifies which land belongs to the farmer and stores its exact
official boundary. This happens once per land parcel. The boundary is then used
for two things: (1) geofencing during AR scans to confirm farmer is on their
own land, and (2) satellite pixel clipping to calculate biomass only within the
verified boundary.

The boundary MUST come from government records. Farmer-drawn polygons are not
acceptable for carbon credit verification by any carbon market standard. The
app auto-fetches the official boundary using a 3-layer system.

4.2 THE 3-LAYER BOUNDARY FETCHING SYSTEM
-----------------------------------------
These layers run in sequence. The app tries Layer 1 first. If it fails, it
tries Layer 2. If that also fails, it falls back to Layer 3. The farmer sees
the same result regardless of which layer succeeded — the boundary displayed on
a map for their confirmation.

LAYER 1 — Automatic via LGD API + BhuNaksha WMS (Primary, tried first)
  How it works:
  1. Farmer uploads photo of their 7/12 Extract or Record of Rights document.
  2. Image sent to backend. PaddleOCR (PP-OCRv5 Devanagari model) extracts:
     Survey Number, Owner Name, Village, Taluka, District, State.
  3. Backend calls Maharashtra LGD API (http://115.124.105.220/API/) with the
     extracted District and Taluka to get official LGD location codes.
     Endpoint used: POST /GetVillagesOfDistrictAndTaluka
     Returns: distcode, talukacode, village_code
  4. Backend constructs the gis_code from: state code + division code + taluka
     code + village code + survey number padded to 6 digits.
  5. Backend calls BhuNaksha WMS GetFeatureInfo endpoint:
     https://mahabhunakasha.mahabhumi.gov.in/WMS?SERVICE=WMS
       &VERSION=1.3.0&REQUEST=GetFeatureInfo
       &INFO_FORMAT=application/json
       &gis_code=[constructed_code]
       &[bounding box and pixel coordinates derived from farmer GPS]
  6. WMS returns GeoJSON with exact polygon coordinates of the survey number.
  7. GeoJSON sent back to app. Boundary shown on map.
  Coverage: All 36 districts of Maharashtra. Other major states have the same
  NIC BhuNaksha software deployed — state router in backend switches the base
  URL per state. Same logic, different domain per state.

LAYER 2 — Web Scraping Fallback
  How it works:
  If Layer 1 WMS returns an error or times out, backend attempts to scrape the
  BhuNaksha portal using Selenium/Playwright with the same extracted fields.
  Result is still GeoJSON. App behaviour is identical.
  Used when: WMS endpoint is temporarily down or returns unexpected response.

LAYER 3 — Manual Guided Upload (Last Resort)
  How it works:
  If both Layer 1 and Layer 2 fail, the app shows the ManualUploadGuideScreen.
  This screen guides the farmer step by step to download their own map from the
  government portal and upload it.
  Steps shown to farmer:
    Step 1: "Open bhunaksha.mahabhumi.gov.in" (shown as tappable link)
    Step 2: "Select your District, Taluka, Village from the menus"
    Step 3: "Find your Survey Number and tap Download"
    Step 4: "Come back here and upload the downloaded image"
  After farmer uploads the image, backend uses OpenCV to extract the polygon
  boundary from the image automatically.
  Result is still GeoJSON. Farmer confirms boundary the same way.
  Used when: State not yet integrated in state router, portal is down, or rural
  area not yet digitised in government system.

IMPORTANT NOTE ON MANUAL POLYGON DRAWING:
The original design had Draw Mode (farmer taps polygon corners) and Walk Mode
(GPS drop every 2 meters) from the previous document. These modes are REMOVED
as primary methods. The official government boundary must be used. Manual
drawing can remain as a hidden developer/admin tool for edge cases only, not
as a farmer-facing feature.

4.3 SCREENS
-----------

SCREEN: LandListScreen
  What farmer sees:  A list of their registered land parcels. Each card shows:
                     - Farm name (e.g., "North Field" — editable by farmer)
                     - Area in acres
                     - Status badge: Verified (green), Pending (orange),
                       Rejected (red)
                     - Last audit date
                     - "Start Audit" button if audit is due this year
  Floating button:   "+" button at bottom right to add a new land parcel.

SCREEN: DocumentUploadScreen
  What farmer sees:  Two large buttons: "Take Photo" and "Upload from Gallery"
                     Instruction: "Take a clear photo of your 7/12 Extract or
                     Record of Rights document"
  After capture:     Preview shown with overlay: "Make sure all text is clearly
                     visible and document is not tilted"
                     "Confirm and Process" button
  Processing:        Loading spinner with text: "Reading your document..."
  After OCR:         Extracted fields shown for farmer confirmation:
                     - Survey Number: [extracted value]
                     - Owner Name: [extracted value]
                     - Village: [extracted value]
                     - Taluka: [extracted value]
                     - District: [extracted value]
                     Two buttons: "This is correct — Continue" and "Try Again"
  If owner name does not match KYC name:
                     Show error in red: "The name on this document does not
                     match your registered name. Please use the land document
                     where you are listed as the owner."
  Library used:      react-native-vision-camera for capture.

SCREEN: BoundaryConfirmScreen
  What farmer sees:  Full-screen map view showing a real satellite photograph
                     of their land with the official boundary polygon drawn on
                     top in green.
  How the satellite image works:
                     This screen does NOT use Google Maps satellite tiles.
                     Instead, the backend generates a static Sentinel-2 true
                     colour PNG image of the farm area via Google Earth Engine
                     (the same GEE account already used for biomass work).
                     The backend returns a URL to this PNG image along with the
                     GeoJSON boundary. The app loads the PNG as a background
                     image inside a react-native-maps component set to
                     mapType="none", then draws the GeoJSON polygon overlay
                     on top using the standard <Polygon> component.
                     Why this matters: Sentinel-2 imagery is free with no
                     per-request cost, often more recent than Google Maps
                     satellite tiles for rural India, and shows agricultural
                     land very clearly (field boundaries, crop rows visible).
  Bottom sheet shows:
                     - Survey Number
                     - Area in acres (calculated from polygon)
                     - Owner Name
                     Large green button: "Yes, this is my land — Confirm"
                     Small red text link below: "This boundary is wrong —
                     Report and Try Again"
  On confirm:        GeoJSON polygon saved to Supabase PostGIS under this
                     farmer's user ID. Land status set to Verified.
                     Navigate back to LandListScreen. New land shown as
                     Verified.
  On reject:         Navigate back to DocumentUploadScreen to restart.

SCREEN: ManualUploadGuideScreen
  Shown only when:   Layer 1 and Layer 2 both fail.
  What farmer sees:  Step-by-step instructions with screenshots to download
                     their map from government portal.
                     Progress indicator: Step 1, Step 2, Step 3, Step 4.
                     Upload button at bottom.
  After upload:      Same processing as Layer 2. Leads to BoundaryConfirmScreen.

================================================================================
SECTION 5: MODULE 3 — AR TREE SCANNING (AUDIT)
================================================================================

5.1 OVERVIEW
------------
Once per year, the farmer performs an audit. They walk to 3 sampling zones
on their land and scan trees at each zone. The measurements collected are:
  - Tree species (from TensorFlow Lite, on-device)
  - Trunk diameter in cm (from ARCore or manual string method)
  - GPS location of each tree
  - One evidence photo per tree

Tree HEIGHT is NOT collected by the farmer in the primary flow. Height comes
from NASA GEDI satellite data which the backend fetches automatically. AR height
measurement (tapping base and top with anchors) is used only as a fallback
when GEDI has no laser footprint data at that location.

5.2 HOW SAMPLING ZONES ARE DETERMINED
--------------------------------------
Before the farmer starts walking, the backend:
1. Fetches Sentinel-2 optical satellite imagery for the farm boundary.
2. Calculates NDVI (Normalized Difference Vegetation Index) for every pixel
   inside the boundary.
3. Identifies 3 distinct vegetation density zones: high density, medium
   density, low density — based on NDVI value clustering.
4. Places one sampling point at the centre of each zone.
5. Calculates a linear walking path through all 3 zone points (shortest route
   through all points, like a transect).
6. Returns GPS coordinates of the 3 zone centres to the app.

The number of zones and the plot radius scale with farm size:
  Up to 1 acre:    2-3 zones, 7 metre radius per zone
  1 to 3 acres:    3-4 zones, 9 metre radius per zone
  3 to 10 acres:   4-6 zones, 11 metre radius per zone
  Above 10 acres:  6-8 zones, 11 metre radius, split into 2 sessions on
                   different days. Progress saved between sessions.

Farmer scans every tree inside the zone radius (shown as a circle on map).
Minimum 3 trees per zone. Maximum 5 trees per zone. If fewer than 3 eligible
trees exist in a zone, the zone is marked as low-density and the algorithm
uses satellite data alone for that zone's biomass estimate.

5.3 THE 3-TIER AR MEASUREMENT SYSTEM
--------------------------------------
The app checks ARCore availability once on startup and stores the result in
Redux (audit.arTier). This check determines which measurement method is used
for the entire audit session.

TIER 1 — Full Depth AR (Phones with hardware depth sensor support)
  Applicable to:    High-end phones above approx. Rs 15,000 that have a
                    Time-of-Flight (ToF) sensor or processors powerful enough
                    for dense depth estimation. ARCore Depth API returns
                    SUPPORTED_INSTALLED with high confidence depth maps.
  What farmer does: Hold phone 1-2 metres from tree trunk, facing the trunk
                    directly. Tap "Measure". Hold still for 3 seconds.
  What happens:     ARCore captures a 16-bit raw depth map. Kotlin code
                    identifies the cluster of 3D points that form the trunk in
                    the centre of the frame. RANSAC algorithm fits a 3D
                    cylinder to those points. Diameter = cylinder radius × 2.
  Validation:       If RANSAC cylinder fit error exceeds 5%, show prompt:
                    "Move closer to the tree and hold still, then tap Measure
                    again."
  Accuracy:         ±2-3 cm error.
  UI indicator:     Green badge shown after measurement: "High Precision"

TIER 2 — Motion-Based AR (ARCore phones without dedicated depth sensor)
  Applicable to:    Mid-range ARCore-compatible phones without a ToF sensor.
                    ARCore uses SLAM (Simultaneous Localization and Mapping)
                    to calculate depth from camera motion combined with
                    gyroscope and accelerometer data. No special hardware
                    needed — same ARCore Depth API, lower quality output.
  What farmer does: Hold phone 1-2 metres from trunk. Tap "Measure". Slowly
                    move phone left and right for 5 seconds.
                    App shows animated arrow: ← → to guide the motion.
  What happens:     SLAM builds a 3D model from the parallax between frames as
                    the phone moves. RANSAC cylinder fit runs on the resulting
                    point cloud. Same diameter extraction as Tier 1.
  Accuracy:         ±4-5 cm error.
  UI indicator:     Blue badge shown after measurement: "Standard Precision"

TIER 3 — Manual String Measurement (Any phone, no ARCore required)
  Applicable to:    Phones where ARCore returns UNSUPPORTED or NOT_AVAILABLE.
                    Budget phones below approx. Rs 8,000, very old devices.
  What farmer does: App shows ManualMeasureScreen with a diagram showing:
                    "Wrap a string or cloth around the tree at chest height.
                    Mark where the string meets itself. Lay it flat and
                    measure the length in centimetres."
                    Farmer enters this length (circumference) in a number input.
  What happens:     App calculates: diameter_cm = circumference_cm / 3.14159
                    This is scientifically identical to measuring with a girth
                    tape, which is the standard method used by professional
                    foresters worldwide.
  Accuracy:         Exact, provided farmer measures string correctly.
  UI indicator:     Grey badge: "Manual Measurement"

DETECTION CODE:
  // features/ar-audit/arTierDetector.ts
  export const detectARTier = async (): Promise<1 | 2 | 3> => {
    const support = await NativeModules.ARModule.checkDepthSupport();
    // Returns: 'FULL_DEPTH', 'SLAM_ONLY', or 'UNSUPPORTED'
    if (support === 'FULL_DEPTH') return 1;
    if (support === 'SLAM_ONLY') return 2;
    return 3;
  };

5.4 SCREENS
-----------

SCREEN: AuditStartScreen
  What farmer sees:  Land name, area, last audit date.
                     Explanation: "You will walk to [N] locations on your land
                     and scan trees at each one. This takes about 20-30 minutes."
                     Estimated walking distance shown.
                     Large green button: "Start Audit"
  On start:          API call to backend to generate satellite-guided zones.
                     Loading state: "Preparing your scanning map..."
                     On response: Navigate to ZoneNavigationScreen with zones.

SCREEN: ZoneNavigationScreen
  Map used:          Google Maps road map via react-native-maps with
                     mapType="standard". This is the only screen that uses
                     Google Maps rendering. Road map view is correct here —
                     the farmer needs to see paths, field edges, and their
                     GPS position clearly while walking. Satellite view is
                     not needed for navigation.
  Map tile caching:  When ZoneNavigationScreen first loads, map tiles for
                     the land boundary area are automatically cached by
                     react-native-maps. This allows the navigation map to
                     work offline (no internet required while walking).
  What farmer sees:  Full map showing:
                     - Blue dot: farmer's real-time GPS location
                     - Green circle with label "Zone A": current target zone
                     - Grey circles: completed zones
                     - Dotted line: planned walking path through all zones
                     Bottom card: "Walk to Zone A — [X] metres ahead"
                     Direction arrow pointing toward target zone.
  Button state:      "Start Scanning in Zone A" button is grey and disabled
                     until farmer is within 10 metres of zone centre.
                     On arrival: Button turns green. Haptic vibration.
                     Text changes to: "You're here — Start Scanning"
  Geofencing check:  At all times, app checks if farmer GPS is inside registered
                     land boundary (stored in Supabase PostGIS, cached locally).
                     If farmer is outside their boundary:
                     Show orange warning banner: "You appear to be outside your
                     registered land boundary. Please return to your land to scan."
                     Scanning button remains disabled until farmer returns inside.

SCREEN: ARCameraScreen
  What farmer sees:  Full-screen camera view.
                     White crosshair reticle at centre of screen.
                     Top status text: updates through states:
                       "Point camera at tree trunk"
                       "Trunk detected"
                       "Measuring..."
                       "Measurement complete"
                     When trunk is detected (Tier 1/2 only): Green wireframe
                     cylinder overlaid on trunk in AR.
                     Bottom area: Three action buttons always visible.
  Action buttons:    1. "Identify Species" — captures still frame, runs
                        TensorFlow Lite species model on-device.
                     2. "Measure Diameter" — triggers Tier 1, 2, or 3 based
                        on detected tier. For Tier 3 this opens
                        ManualMeasureScreen.
                     3. "Measure Height" — shown only when GEDI has no data
                        for this location (backend flags this during zone
                        generation). Uses AR anchor method: farmer taps base
                        of tree, then taps top. Height = vertical distance
                        between anchors.
  Progress counter:  Top-right corner shows: "3/9 trees scanned"

SPECIES IDENTIFICATION FLOW:
  Farmer taps "Identify Species".
  App captures still frame from camera.
  TensorFlow Lite model runs on-device (no internet needed).
  Result shown as overlay: "Teak (Tectona grandis) — 87% confident"
  If confidence is below 60%:
    Show dropdown list of approved species for manual selection.
  If species is not in approved list:
    Show error: "This species is not eligible for carbon credits. Please scan
    a different tree."
    Approved species list is defined in common/constants/species.ts
    Includes: Teak, Bamboo, Eucalyptus, Neem, Mango, Pongamia, Subabul,
    Casuarina, and other regionally approved agroforestry species.
    The wood density (rho value) for Chave's equation is looked up from this
    list automatically — farmer never enters wood density manually.

DIAMETER MEASUREMENT FLOW (Tier 1 and Tier 2):
  Farmer taps "Measure Diameter".
  Haptic vibration confirms scan start.
  Tier 1: Progress ring fills over 3 seconds. Phone stays still.
  Tier 2: Animated arrow appears guiding left-right motion over 5 seconds.
  On success: Green cylinder snaps to trunk. Haptic vibration.
  Result shown in bottom sheet: "Diameter: 24 cm"
  If fit error > 5%: "Move closer to the tree and try again."
  If result < 5 cm or > 200 cm: "This seems unusual. Please measure again."

SCREEN: ManualMeasureScreen (Tier 3 only)
  What farmer sees:  Clear diagram showing string wrapped around tree trunk.
                     Instruction: "Wrap a string around the tree at chest
                     height. Mark where it meets itself. Measure that length."
                     Number input field for entering centimetres.
                     "Calculate Diameter" button.
  On submit:         App shows calculated diameter: "Diameter: [X] cm
                     (calculated from [input] cm circumference)"
                     Confirm button to accept and return to ARCameraScreen.

SCREEN: TreeResultScreen
  What farmer sees:  Summary card showing:
                     - Species name
                     - Diameter in cm
                     - Height: "From GEDI Satellite" OR "AR Measured: [X] m"
                     - Precision badge (High / Standard / Manual)
                     - GPS coordinates in readable format: "18.5460°N, 73.9820°E"
                     - Evidence photo thumbnail (auto-captured by app)
  Two buttons:       "Confirm and Save Tree" (green) — saves to Redux and local
                     MMKV storage immediately. Not dependent on internet.
                     "Rescan This Tree" (outline) — returns to ARCameraScreen.
  On save:           Progress counter updates. If minimum trees in this zone are
                     done (3), show option to proceed to next zone or scan more.

SCREEN: AuditCompleteScreen
  What farmer sees:  Completion summary:
                     - Total trees scanned: [N]
                     - Zones completed: [N] of [N]
                     - Preliminary carbon estimate (rough preview, clearly
                       labelled as "Estimated — final number calculated by
                       satellite verification")
                     Large button: "Submit for Satellite Verification"
  On submit:         All tree data + GPS locations + evidence photos uploaded
                     to backend. SHA-256 hash of each photo computed before
                     upload and stored.
                     Loading screen: "Calculating your carbon credits using
                     satellite data... This takes about 30-60 seconds."
                     Backend runs full satellite fusion (Sentinel-1, Sentinel-2,
                     GEDI, NISAR, SRTM, XGBoost model).
  On completion:     Navigate to HomeScreen. Success animation plays.
                     New CTT balance shown. Blockchain transaction hash
                     available in audit history.

================================================================================
SECTION 6: MODULE 4 — DASHBOARD
================================================================================

6.1 SCREEN: HomeScreen
-----------------------
The main screen the farmer sees every time they open the app.

Design goal: A farmer who does not understand blockchain, carbon markets, or
satellite imagery must be able to look at this screen and understand exactly
what they have earned and what they need to do next.

TOP SECTION — Token Balance Card:
  Large number:      "[47.3] CTT" in the largest font on screen
  Subtitle:          "Carbon Ton Tokens earned"
  Explanation line:  "= 47.3 tonnes of CO2 stored on your land"
  No wallet addresses. No blockchain terminology. Just a number.
  "View on PolygonScan" small link for those who want blockchain proof.

MIDDLE SECTION — My Lands:
  List of registered land parcels.
  Each card shows: Thumbnail satellite image of land (GEE Sentinel-2 PNG,
  fetched once from backend and cached locally), Name, Area, Status.
  Status colours:
    Green = Verified and audited this year
    Orange = Verified but audit due
    Red = Overdue by more than 3 months
  "Start Audit" quick-action button on orange/red cards.

BOTTOM SECTION — Credit History Preview:
  Tappable row showing last 2 audit entries. Tapping navigates to
  CreditHistoryScreen for the full history.
  Year-by-year preview:
    2025: +12.4 CTT | [View Certificate]
    2024: +9.8 CTT  | [View Certificate]
  "View Certificate" opens the IPFS-hosted audit certificate in browser.
  "View All History →" link navigates to CreditHistoryScreen.

6.2 SCREEN: CreditHistoryScreen
---------------------------------
Full audit history for the farmer across all land parcels.

What farmer sees:  Year-by-year list of all carbon credit audits.
                   Each entry shows:
                     Land name (e.g. "North Field")
                     Audit year (e.g. 2025)
                     Credits issued (e.g. +12.4 CTT)
                     "View Certificate" button — opens IPFS URL in browser.
                     Transaction hash (truncated, tappable → PolygonScan).
  Bar chart:       react-native-chart-kit bar chart showing credit growth
                   year over year across all parcels.
  Loading state:   Lottie spinning_leaf.json while fetching from API.
  Offline state:   Shows cached last known history with "Last updated [time]"
                   badge. Data persisted in Redux credits.history via MMKV.
  API call:        GET /api/v1/credits/balance (returns balance + full history)

================================================================================
SECTION 7: STATE MANAGEMENT (REDUX TOOLKIT)
================================================================================

7.1 STORE STRUCTURE
--------------------

  {
    auth: {
      user: {
        id: string,
        name: string,
        phone: string,
        aadhaar_hash: string     // Hashed, never plain text
      },
      walletAddress: string,     // Public blockchain address only
      isAuthenticated: boolean,
      kycCompleted: boolean
    },

    land: {
      parcels: LandParcel[],     // All verified land parcels
      currentDraft: {            // Land being registered right now
        ocrResult: {             // What OCR extracted from document
          surveyNumber: string,
          ownerName: string,
          village: string,
          taluka: string,
          district: string,
          state: string
        },
        boundary: GeoJSONPolygon | null,
        fetchStatus: 'idle' | 'fetching' | 'success' |
                     'manual_required' | 'error'
      }
    },

    audit: {
      activeAuditId: string | null,
      activeLandId: string | null,
      zones: SamplingZone[],     // GPS coords from satellite analysis
      currentZoneIndex: number,
      scannedTrees: TreeSample[], // CRITICAL — must be persisted
      arTier: 1 | 2 | 3,        // Detected on app startup, stored here
      sessionComplete: boolean,   // For multi-day audits on large farms
      uploadStatus: 'idle' | 'uploading' | 'processing' | 'success' | 'error'
    },

    credits: {
      balance: number,           // Total CTT tokens
      history: AuditRecord[],    // Past audits with tx hashes
      pendingMint: boolean       // True while blockchain tx is processing
    }
  }

7.2 WHAT MUST BE PERSISTED
---------------------------
Use redux-persist with MMKV as the storage adapter.
MMKV must be used instead of AsyncStorage because:
  - 30x faster read/write speed
  - Synchronous API — tree scan data saves instantly when farmer taps "Save"
  - Encrypted storage — private data like audit measurements is protected

The following state slices MUST be persisted to MMKV:

  audit.scannedTrees      — Most critical. If farmer scans 8 trees and the app
                            crashes or phone dies, all 8 trees must be there
                            when they reopen the app. Losing this data means the
                            farmer must redo the entire audit.
  audit.activeAuditId     — App must know an audit is in progress.
  audit.currentZoneIndex  — Farmer must not re-navigate to completed zones.
  audit.arTier            — Avoid rechecking ARCore availability every launch.
  auth.walletAddress      — Avoid re-fetching from server every launch.
  auth.isAuthenticated    — Avoid forcing re-login every launch.
  land.parcels            — Cache land list for offline viewing.

The following must NOT be persisted:
  audit.uploadStatus      — Reset to 'idle' on each app launch.
  land.currentDraft       — Clear after registration is complete.

================================================================================
SECTION 8: API INTEGRATION
================================================================================

8.1 AXIOS CONFIGURATION
------------------------

  // services/api.ts
  const api = axios.create({
    baseURL: Config.API_BASE_URL,    // From .env via react-native-config
    timeout: 60000,                  // 60 seconds — satellite calls can be slow
  });

  // Auto-attach Supabase JWT token to every request
  api.interceptors.request.use(async (config) => {
    const { data: { session } } = await supabase.auth.getSession();
    if (session) {
      config.headers.Authorization = `Bearer ${session.access_token}`;
    }
    return config;
  });

  // Global response error handling
  api.interceptors.response.use(
    (response) => response,
    (error) => {
      if (error.response?.status === 401) {
        // Token expired — redirect to login
        navigateTo('LoginScreen');
      } else if (error.response?.status === 500) {
        // Server error — show maintenance banner
        showBanner('Server issue. Please try again in a few minutes.');
      } else if (!error.response) {
        // No internet — show offline banner
        showBanner('No internet connection. Your data is saved locally.');
      }
      return Promise.reject(error);
    }
  );

8.2 API ENDPOINTS USED BY THE APP
-----------------------------------

All endpoints are prefixed with /api/v1. The API_BASE_URL in .env already
points to the server root, so the Axios instance uses the full path for each call.

POST /api/v1/auth/kyc
  Called when:  Farmer submits KYC form on KYCScreen after first login.
  Sends:        { full_name: string, aadhaar_number: string }
  Receives:     { status: 'success', user_id: UUID }
  Note:         Backend hashes Aadhaar with SHA-256. Never stored plain text.

POST /api/v1/auth/register-wallet
  Called when:  Silent wallet creation completes after first OTP verification.
  Sends:        { wallet_address: string }  // Public address only, never private key
  Receives:     { status: 'success' }

POST /api/v1/land/verify-document
  Called when:  Farmer submits document photo from DocumentUploadScreen.
  Sends:        Image as base64 string.
  Receives:     { survey_number, owner_name, village, taluka, district, state }

GET /api/v1/land/fetch-boundary
  Called when:  OCR confirms extracted fields. Auto-called in background.
  Sends:        survey_number, district, taluka, village, state, user_lat, user_lng
  Receives:     {
                  status: 'success',
                  boundary_source: 'WMS_AUTO' | 'SCRAPE' | 'MANUAL',
                  geojson: GeoJSONPolygon,
                  satellite_thumbnail_url: string
                }
                or { status: 'manual_required' } if all layers fail

POST /api/v1/land/register
  Called when:  Farmer confirms boundary on BoundaryConfirmScreen.
  Sends:        {
                  farm_name: string,
                  survey_number: string,
                  district: string,
                  taluka: string,
                  village: string,
                  state: string,
                  boundary_source: string,
                  geojson: GeoJSONPolygon,
                  ocr_owner_name: string
                }
  Receives:     { land_id: UUID, area_hectares: number, status: 'verified' }

GET /api/v1/land/list
  Called when:  LandListScreen loads.
  Sends:        (auth token only — user identified from JWT)
  Receives:     Array of { id, farm_name, survey_number, area_hectares,
                  is_verified, last_audit_year, thumbnail_url }

GET /api/v1/audit/zones
  Called when:  Farmer taps "Start Audit" on AuditStartScreen.
  Sends:        land_id
  Receives:     {
                  audit_id: UUID,
                  zones: [{ zone_id, label, centre_gps, radius_metres,
                    zone_type, walking_path_metres, min_trees_required }]
                }
                zone_type is 'high_density', 'medium_density', or 'low_density'

POST /api/v1/audit/submit-samples
  Called when:  Farmer taps "Submit" on AuditCompleteScreen.
  Sends:        {
                  land_id: UUID,
                  audit_id: UUID,
                  trees: [
                    {
                      zone_id: UUID,
                      species: string,
                      dbh_cm: number,
                      height_m: number | null,       // null = use GEDI
                      gps: { lat: number, lng: number },
                      ar_tier_used: 1 | 2 | 3,
                      confidence_score: number,       // 0.0–1.0 RANSAC quality
                      evidence_photo_base64: string,  // full photo data
                      evidence_photo_hash: string     // SHA-256 of photo
                    }
                  ]
                }
  Receives:     { status: 'processing', audit_id: UUID, estimated_seconds: 60 }

GET /api/v1/audit/result/{audit_id}
  Called when:  Polling every 5 seconds after submission.
  Receives (processing):  { status: 'CALCULATING' }
  Receives (success):     {
                            status: 'MINTED',
                            total_biomass_tonnes: number,
                            credits_issued: number,
                            tx_hash: string,
                            ipfs_certificate_url: string,
                            audit_year: number
                          }
  Receives (failed):      { status: 'FAILED', error: string }

GET /api/v1/audit/history/{land_id}
  Called when:  CreditHistoryScreen loads for a specific land parcel.
  Receives:     Array of { audit_year, total_biomass_tonnes, credits_issued,
                  tx_hash, ipfs_certificate_url, minted_at }

GET /api/v1/credits/balance
  Called when:  HomeScreen and CreditHistoryScreen load.
  Sends:        wallet_address from auth state (query param)
  Receives:     {
                  balance_ctt: number,
                  history: [{
                    audit_year: number,
                    credits_issued: number,
                    land_name: string,
                    tx_hash: string,
                    ipfs_certificate_url: string,
                    minted_at: string
                  }]
                }

================================================================================
SECTION 9: UI/UX DESIGN SYSTEM
================================================================================

9.1 DESIGN PRINCIPLES
----------------------
1. One primary action per screen. Farmer never faces two choices of equal weight.
2. Progress always visible. Farmer always knows: "I am at step 2 of 4."
3. Errors in plain language. "Something went wrong, please try again" not
   "HTTP 500 Internal Server Error."
4. Works without internet for all field operations (scanning, navigation).
5. Any farmer, regardless of age or literacy, must complete the process
   independently without assistance. Every instruction uses simple words.

9.2 COLOUR PALETTE
-------------------

Name                 | Hex Code | Used For
---------------------|----------|------------------------------------------
Forest Green (Primary)| #2F855A | Confirm buttons, success states, verified
                     |          | badges, headings, active tab indicator
Dark Slate (Text)    | #2D3748  | All body text, headings
Teal (Accent)        | #38B2AC  | AR overlays, active zone circles on map,
                     |          | GEDI height badge
Error Red            | #E53E3E  | Invalid boundary, scan failed, name
                     |          | mismatch errors, overdue status
Warning Orange       | #DD6B20  | Audit due reminders, low confidence scan
                     |          | warnings, outside boundary warning
Off-white (Background)| #F7FAFC | Main app background
Card White           | #FFFFFF  | Card backgrounds, bottom sheets
Disabled Grey        | #A0AEC0  | Disabled buttons, placeholder text,
                     |          | completed zone circles on map

9.3 TYPOGRAPHY
---------------

Role             | Font         | Size | Weight     | Used For
-----------------|--------------|------|------------|---------------------
Screen Title     | Roboto       | 24sp | Bold       | Top of each screen
Section Header   | Roboto       | 18sp | Semi-Bold  | Within screen sections
Body Text        | Roboto       | 16sp | Regular    | Instructions, desc.
Button Label     | Roboto       | 16sp | Medium     | All button text
Caption          | Roboto       | 12sp | Regular    | Helper text, coords
Measurement Value| Roboto Mono  | 32sp | Bold       | Diameter/height after
                 |              |      |            | successful scan
Balance Number   | Roboto       | 48sp | Bold       | CTT balance on Home

9.4 COMPONENT SPECIFICATIONS
-----------------------------

Buttons:
  Primary Button:
    Height: 52px, Width: full-width of screen (minus 32px margins)
    Background: #2F855A (Forest Green)
    Text: White, Roboto Medium 16sp
    Border Radius: 12px
    Disabled state: Background #A0AEC0, text white

  Secondary Button:
    Same dimensions as Primary.
    Background: White
    Border: 1.5px solid #2F855A
    Text: #2F855A

  Destructive Button (e.g., "Report and Try Again"):
    Same dimensions.
    Background: White
    Border: 1.5px solid #E53E3E
    Text: #E53E3E

  All touch targets: Minimum 48px height (accessibility standard).

Input Fields:
  Style: Outlined with floating label (label moves up when focused)
  Height: 56px
  Border: 1px solid #A0AEC0 when unfocused, 2px solid #2F855A when focused
  Border Radius: 8px
  Error state: Border turns #E53E3E, red helper text appears below

Cards:
  Background: White
  Border Radius: 12px
  Shadow: elevation 2 (Android)
  Padding: 16px

Bottom Sheets:
  Slide up from bottom of screen.
  Top border radius: 24px
  Drag handle: 4px × 32px grey pill at top centre
  Background: White

Loading State:
  Custom Lottie animation: spinning_leaf.json (green leaf spinning)
  Shown centred on screen with semi-transparent overlay when:
    - Document OCR processing
    - Boundary fetching
    - Audit submission and satellite calculation

Status Badges:
  Verified:  Green background (#C6F6D5), green text (#276749), "✓ Verified"
  Pending:   Orange background (#FEFCBF), orange text (#975A16), "⏳ Pending"
  Rejected:  Red background (#FED7D7), red text (#822727), "✗ Rejected"
  High Precision: Green background, "◉ High Precision"
  Standard:  Blue background, "◉ Standard Precision"
  Manual:    Grey background, "◎ Manual Measurement"

Map Overlays:
  --- BoundaryConfirmScreen (GEE Sentinel-2 satellite background) ---
  Satellite background:   GEE PNG loaded as background image in mapType="none"
  Verified land boundary: Semi-transparent green fill (#2F855A at 20% opacity)
                          Green border 2px solid drawn using <Polygon>

  --- ZoneNavigationScreen (Google Maps road map) ---
  Map type:               mapType="standard" (road map, not satellite)
  Land boundary outline:  Green border 1.5px dashed (boundary always visible
                          so farmer knows their land edges while walking)
  Current target zone:    Teal circle fill (#38B2AC at 15% opacity)
                          Teal border 2px dashed, label "Zone A" in centre
  Completed zone:         Grey fill (#A0AEC0 at 15% opacity), no label
  Walking path:           Green dotted line connecting all zone centres
  Farmer location:        Standard blue dot (Google Maps default)
  Tree scan location:     Small green pin marker dropped after each scan

  --- LandListScreen (no map component — static image only) ---
  Land thumbnail:         GEE Sentinel-2 PNG displayed as a static
                          <Image> component. Not interactive. Cached after
                          first fetch per land parcel.

================================================================================
SECTION 10: NATIVE AR BRIDGE (KOTLIN)
================================================================================

10.1 PURPOSE
------------
React Native JavaScript code cannot directly call Google ARCore's depth and
point cloud APIs. A Native Module written in Kotlin acts as a bridge. The
Kotlin code does all ARCore work and exposes simple functions that JavaScript
calls as if they were regular async functions.

10.2 TYPESCRIPT INTERFACE
--------------------------

  // services/ar-bridge.ts
  interface ARMeasurementResult {
    diameter_cm: number;       // Calculated trunk diameter
    confidence: number;        // 0.0 to 1.0 — RANSAC fit quality
    tier_used: 1 | 2;         // 1 = hardware depth, 2 = SLAM motion
    point_count: number;       // Number of 3D points used in calculation
  }

  // Measures tree trunk diameter using ARCore
  export const measureTreeDiameter = async (): Promise<ARMeasurementResult> => {
    const result = await NativeModules.ARModule.measureCylinder();
    return JSON.parse(result);
  };

  // Checks what level of depth support this phone has
  export const detectARTier = async (): Promise<1 | 2 | 3> => {
    const support = await NativeModules.ARModule.checkDepthSupport();
    if (support === 'FULL_DEPTH') return 1;
    if (support === 'SLAM_ONLY') return 2;
    return 3; // UNSUPPORTED
  };

10.3 KOTLIN MODULE IMPLEMENTATION (ARModule.kt)
-------------------------------------------------

The ARModule.kt file must:

1. Extend ReactContextBaseJavaModule
   class ARModule(reactContext: ReactApplicationContext)
     : ReactContextBaseJavaModule(reactContext)

2. Override getName() to return "ARModule"
   This is the name used in NativeModules.ARModule in JavaScript.

3. Implement checkDepthSupport() with @ReactMethod annotation
   Check: ArCoreApk.checkAvailability(context)
   Check: session.isDepthModeSupported(Config.DepthMode.RAW_DEPTH_ONLY)
   Return string: 'FULL_DEPTH', 'SLAM_ONLY', or 'UNSUPPORTED'

4. Implement measureCylinder() with @ReactMethod annotation
   Step 1: Configure ARCore session:
           config.setDepthMode(Config.DepthMode.RAW_DEPTH_ONLY)
           RAW_DEPTH_ONLY is used, not AUTOMATIC, because raw depth gives
           more accurate values. Raw depth does not fill missing pixels, which
           means only confident depth measurements are used.
   Step 2: Access depth data:
           val depthImage = frame.acquireRawDepthImage16Bits()
   Step 3: Filter to centre region of frame (the reticle area, approximately
           the centre 30% of width and 60% of height).
   Step 4: Convert depth pixels to 3D world coordinates using camera intrinsics.
   Step 5: Run RANSAC cylinder fitting loop:
           - Randomly sample 2 points to define an axis direction
           - Randomly sample 1 point to define radius
           - Count how many remaining points fall within epsilon distance of
             the cylinder surface (these are "inliers")
           - If inlier count > best so far, update best cylinder
           - Repeat N times (N = 100 iterations minimum)
   Step 6: Calculate confidence = best_inlier_count / total_points
           If confidence < 0.7, return error: fit quality too low.
   Step 7: Return JSON:
           {"diameter_cm": X, "confidence": Y, "tier_used": 1or2,
            "point_count": Z}

5. Register ARModule in ARPackage.kt
   ARPackage must implement ReactPackage and return ARModule in
   createNativeModules().
   ARPackage must be added to getPackages() in MainApplication.kt.

10.4 ANDROID MANIFEST ADDITIONS
---------------------------------

In android/app/src/main/AndroidManifest.xml:

  <!-- Permissions -->
  <uses-permission android:name="android.permission.CAMERA" />
  <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
  <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
  <uses-permission android:name="android.permission.INTERNET" />

  <!-- Declare AR hardware feature -->
  <!-- required="false" allows the app to install on non-AR phones -->
  <!-- Tier 3 (manual string measurement) works on all phones -->
  <uses-feature
    android:name="android.hardware.camera.ar"
    android:required="false" />

  <!-- Inside <application> tag -->
  <!-- ARCore: "optional" means app works without ARCore (shows Tier 3 flow) -->
  <meta-data
    android:name="com.google.ar.core"
    android:value="optional" />

  <!-- Google Maps API key (for ZoneNavigationScreen only) -->
  <!-- Value injected from local.properties via build.gradle -->
  <meta-data
    android:name="com.google.android.geo.API_KEY"
    android:value="${GOOGLE_MAPS_API_KEY}" />

10.5 BUILD.GRADLE ADDITIONS (app level)
-----------------------------------------

  // At the TOP of android/app/build.gradle, add these lines to read
  // local.properties (this is how the Maps API key is injected securely
  // without committing it to git):
  def localProperties = new Properties()
  def localPropertiesFile = rootProject.file('local.properties')
  if (localPropertiesFile.exists()) {
      localPropertiesFile.withReader('UTF-8') { reader ->
          localProperties.load(reader)
      }
  }

  // Inside the android { defaultConfig { ... } } block, add:
  manifestPlaceholders = [
      GOOGLE_MAPS_API_KEY: localProperties.getProperty("GOOGLE_MAPS_API_KEY", "")
  ]

  // In android/local.properties (create if it does not exist — already in
  // .gitignore by default), add:
  // GOOGLE_MAPS_API_KEY=AIzaSyBxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

  dependencies {
    // ARCore SDK
    implementation 'com.google.ar:core:1.42.0'

    // TensorFlow Lite for on-device species detection
    implementation 'org.tensorflow:tensorflow-lite:2.14.0'
    implementation 'org.tensorflow:tensorflow-lite-support:0.4.4'
  }

================================================================================
SECTION 11: SECURITY
================================================================================

THREAT: Farmer scans trees outside their registered land
PROTECTION: useGeofence hook checks real-time GPS against PostGIS boundary on
every GPS update during audit. Scanning button disabled if outside boundary.
Message shown: "You appear to be outside your registered land. Please return
to your land to scan."

THREAT: Farmer uses a fake/mocked GPS location
PROTECTION: On audit start, Kotlin code checks:
  Settings.Secure.getInt(contentResolver,
    Settings.Secure.ALLOW_MOCK_LOCATION, 0)
If mock locations are enabled, show blocking screen:
"Please disable Mock Location in Developer Settings to use this app."
Audit cannot start until mock location is disabled.

THREAT: Evidence photo tampered with after upload
PROTECTION: SHA-256 hash of each evidence photo computed on-device before
upload. Hash sent to backend along with photo. Hash stored on blockchain.
Any post-upload modification to the photo produces a different hash that
no longer matches the on-chain record.
  // common/utils/hash.ts
  import { createHash } from 'react-native-quick-crypto';
  export const hashPhoto = (base64Data: string): string => {
    return createHash('sha256').update(base64Data).digest('hex');
  };

THREAT: Private key exposure
PROTECTION: Private key stored ONLY in react-native-keychain using Android
Keystore hardware-backed encryption. Never stored in Redux state. Never stored
in AsyncStorage or MMKV. Never logged. Never sent to any server.

THREAT: JWT token theft
PROTECTION: Supabase JWT tokens have 1-hour expiry and auto-refresh. Stored
in MMKV (encrypted) not AsyncStorage (plain text).

THREAT: App running on rooted phone
PROTECTION: react-native-device-info checks isRooted(). If rooted device
detected, show warning: "This device appears to be rooted. For your security,
some features may not work correctly." App continues — not blocked, because
some legitimate users have custom ROMs.

================================================================================
SECTION 12: OFFLINE STRATEGY
================================================================================

Feature                 | Works Offline? | How
------------------------|----------------|----------------------------------
Species identification  | YES            | TensorFlow Lite runs on-device.
                        |                | No internet needed.
AR diameter measurement | YES            | ARCore runs on-device hardware.
GPS zone navigation     | YES            | GPS is device hardware. Google
                        |                | Maps road tiles cached when
                        |                | ZoneNavigationScreen first loads.
GEE satellite image     | CACHED         | Sentinel-2 PNG fetched once after
                        |                | land is registered. Cached locally
                        |                | in MMKV. BoundaryConfirmScreen and
                        |                | land thumbnails work offline after
                        |                | first load.
Tree data saving        | YES            | Saved to Redux + MMKV locally.
                        |                | Uploaded when internet returns.
Document OCR            | NO             | Requires backend PaddleOCR server.
Boundary API fetch      | NO             | Requires WMS API call.
Audit submission        | QUEUED         | Background fetch service detects
                        |                | connectivity and auto-uploads.
Dashboard balance       | CACHED         | Last known balance shown. Badge
                        |                | "Last updated [time]" shown.

Background Sync Implementation:
  When farmer submits audit data with no internet, store the full payload in
  MMKV under key 'pending_upload'. react-native-background-fetch detects
  when connectivity is restored and automatically calls
  POST /api/v1/audit/submit-samples with the stored payload.
  After successful upload: clear 'pending_upload' from MMKV and show
  notification: "Your audit results are ready!"
  Farmer does not need to open the app for this to happen.

================================================================================
SECTION 13: TESTING STRATEGY
================================================================================

Test Type            | Tool                            | What to Test
---------------------|----------------------------------|--------------------
Unit Tests           | Jest                            | Redux reducers, all
                     |                                 | utility functions:
                     |                                 | circumference-to-
                     |                                 | diameter calculation,
                     |                                 | GeoJSON polygon area,
                     |                                 | SHA-256 hashing,
                     |                                 | Chave equation preview
Component Tests      | React Native Testing Library    | Buttons fire correct
                     |                                 | events. Inputs accept
                     |                                 | text. Error states show
                     |                                 | correct messages.
                     |                                 | Loading states render.
AR Tier Tests        | Physical device only.           | Test Tier 1 on a phone
                     | Emulator cannot run ARCore.     | with ToF sensor (Pixel
                     |                                 | 8 Pro, Samsung S22
                     |                                 | Ultra). Test Tier 2 on
                     |                                 | mid-range phone. Test
                     |                                 | Tier 3 by disabling AR.
AR Accuracy Test     | Measuring tape + known object   | Measure a cylinder of
                     |                                 | known diameter (e.g.,
                     |                                 | a bucket or pipe) with
                     |                                 | tape. Scan same object
                     |                                 | with app. Result must
                     |                                 | be within 5% of actual.
GPS Tests            | Open field                      | Walk to zone markers.
                     |                                 | Verify 10m arrival
                     |                                 | detection accuracy.
                     |                                 | Test geofencing at
                     |                                 | boundary edge.
Offline Test         | Airplane mode during audit      | Complete full audit in
                     |                                 | airplane mode. Force
                     |                                 | close app. Reopen.
                     |                                 | All trees must still be
                     |                                 | there. Restore internet.
                     |                                 | Auto-upload must trigger.
Supabase Auth Test   | Postman + Supabase dashboard    | Full OTP flow. Session
                     |                                 | persistence. Token
                     |                                 | refresh. KYC storage.
Mock GPS Test        | Enable developer mock GPS       | App must detect and
                     |                                 | block scanning.

================================================================================
SECTION 14: ENVIRONMENT SETUP AND BUILD
================================================================================

14.1 REQUIRED ACCOUNTS (ALL FREE)
-----------------------------------

Supabase (supabase.com):
  Action: Create project. In SQL editor run:
            CREATE EXTENSION IF NOT EXISTS postgis;
            CREATE EXTENSION IF NOT EXISTS postgis_topology;
  Get: Project URL and anon key for .env file.
  Provides: PostgreSQL + PostGIS + Phone Auth + File Storage.
  Note: Free tier pauses after 7 days of inactivity. Add a ping every 5 days
        via a cron service (cron-job.org, free) to prevent pausing.

Google Cloud Console (console.cloud.google.com):
  Action: Enable Maps SDK for Android (used only for ZoneNavigationScreen —
          the GPS walking navigation screen).
          Enable ARCore API (no key needed, just enable it).
          Download google-services.json and place in android/app/
  Get: Maps API key for react-native-maps (ZoneNavigationScreen only).
  Important: Also set a daily quota cap of 1,000 requests on the Maps SDK
             for Android in the Quotas section to prevent unexpected billing.
             At MVP scale (few hundred farmers, one audit/year) you will
             comfortably stay within the 10,000 free loads/month limit.
  NOT used for: Boundary confirmation or land thumbnails — those use GEE
                Sentinel-2 images fetched by the backend (free, no limits).

14.2 ENVIRONMENT VARIABLES
----------------------------

Use react-native-config library. Create these files in project root:

  .env.development:
    API_BASE_URL=http://10.0.2.2:8000
    SUPABASE_URL=https://[your-project-ref].supabase.co
    SUPABASE_ANON_KEY=[your-anon-key]
    GOOGLE_MAPS_API_KEY=[your-maps-key]

  .env.production:
    API_BASE_URL=https://[your-production-domain]
    SUPABASE_URL=https://[your-project-ref].supabase.co
    SUPABASE_ANON_KEY=[your-anon-key]
    GOOGLE_MAPS_API_KEY=[your-maps-key]

IMPORTANT: Never commit .env files to git. Add both to .gitignore.

14.3 PROJECT INITIALISATION
-----------------------------

  npx react-native init TerraTrustAR --template react-native-template-typescript
  cd TerraTrustAR

  # Core dependencies
  npm install @reduxjs/toolkit react-redux redux-persist
  npm install react-native-mmkv
  npm install @react-navigation/native @react-navigation/native-stack
  npm install @react-navigation/bottom-tabs
  npm install react-native-screens react-native-safe-area-context
  npm install react-native-maps
  npm install react-native-vision-camera
  npm install react-native-geolocation-service
  npm install axios
  npm install @supabase/supabase-js
  npm install ethers
  npm install react-native-keychain
  npm install react-native-config
  npm install react-native-background-fetch
  npm install react-native-chart-kit react-native-svg
  npm install lottie-react-native
  npm install react-native-reanimated
  npm install react-native-haptic-feedback
  npm install react-native-device-info
  npm install nativewind
  npm install zod react-hook-form @hookform/resolvers
  npm install --save-dev jest @testing-library/react-native

14.4 BUILD COMMANDS
--------------------

  # Start Metro bundler (development)
  npx react-native start

  # Run on connected Android device (development)
  npx react-native run-android

  # Build release APK
  cd android && ./gradlew assembleRelease
  # Output: android/app/build/outputs/apk/release/app-release.apk

  # Build release AAB (for Play Store)
  cd android && ./gradlew bundleRelease
  # Output: android/app/build/outputs/bundle/release/app-release.aab

14.5 APK SIGNING
-----------------

  # Generate keystore (one time only — keep this file safe)
  keytool -genkey -v -keystore terratrust-release.keystore
          -alias terratrust -keyalg RSA -keysize 2048 -validity 10000

  # In android/gradle.properties, add:
  MYAPP_RELEASE_STORE_FILE=terratrust-release.keystore
  MYAPP_RELEASE_KEY_ALIAS=terratrust
  MYAPP_RELEASE_STORE_PASSWORD=[your password]
  MYAPP_RELEASE_KEY_PASSWORD=[your password]

  # In android/app/build.gradle, add signingConfigs block pointing to above.

================================================================================
SECTION 15: COMPLETE SCREEN FLOW SUMMARY
================================================================================

COMPLETE SCREEN LIST:
  Authentication: SplashScreen, LoginScreen, OTPScreen, KYCScreen
  Land:           LandListScreen, DocumentUploadScreen, BoundaryConfirmScreen,
                  ManualUploadGuideScreen
  Audit:          AuditStartScreen, ZoneNavigationScreen, ARCameraScreen,
                  ManualMeasureScreen, TreeResultScreen, AuditCompleteScreen
  Dashboard:      HomeScreen, CreditHistoryScreen

NEW USER FLOW:
  App opens → SplashScreen [no session] → LoginScreen → OTPScreen
  → [Silent wallet creation in background]
  → KYCScreen → HomeScreen (empty, no lands yet)

RETURNING USER FLOW:
  App opens → SplashScreen [session valid, KYC done] → HomeScreen

ADD NEW LAND FLOW:
  HomeScreen [tap +] → DocumentUploadScreen
  → [OCR runs, LGD API called, BhuNaksha WMS called — all in background]
  → BoundaryConfirmScreen [farmer sees boundary on map]
  → [Farmer confirms] → LandListScreen [land shows as Verified]

  If WMS fails:
  → ManualUploadGuideScreen [farmer downloads and uploads map image]
  → [OpenCV extracts boundary] → BoundaryConfirmScreen → same as above

ANNUAL AUDIT FLOW:
  HomeScreen [tap "Start Audit"] → AuditStartScreen
  → [Backend generates satellite zones from Sentinel-2 NDVI analysis]
  → ZoneNavigationScreen [Zone A — farmer walks to zone]
  → [Arrival detected within 10m] → ARCameraScreen
  → [Scan 3-5 trees: species + diameter each]
  → ZoneNavigationScreen [Zone B] → ARCameraScreen [scan 3-5 trees]
  → ZoneNavigationScreen [Zone C] → ARCameraScreen [scan 3-5 trees]
  → AuditCompleteScreen [farmer reviews summary]
  → [Submit] → [Backend: satellite fusion + NISAR/S1/S2/GEDI/SRTM + XGBoost]
  → [Blockchain: evidence to IPFS → smart contract mint → CTT tokens issued]
  → HomeScreen [updated balance visible, new audit in history]

CREDIT HISTORY FLOW:
  HomeScreen [tap "View All History"] → CreditHistoryScreen
  [tap "View Certificate" on any entry] → IPFS certificate URL opens in browser

LARGE FARM FLOW (above 10 acres — multi-day):
  Same as audit flow but zones are split into 2 sessions.
  Session 1: Zones A, B, C → AuditCompleteScreen shows "Session 1 saved.
  Come back to complete Session 2."
  Session 2 (next day): App resumes at ZoneNavigationScreen for Zone D.
  All Session 1 data persisted in MMKV.

================================================================================
END OF DOCUMENT
================================================================================
