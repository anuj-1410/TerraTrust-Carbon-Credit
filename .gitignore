# Environment variables — NEVER commit these
.env
.env.development
.env.production
.env.local

# Android secrets — NEVER commit
android/local.properties

# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# React Native
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*
web-build/

# Android build outputs
android/app/build/
android/build/
android/.gradle/
android/app/src/main/assets/

# iOS (not needed but safe to include)
ios/Pods/
ios/build/

# TFLite model — large binary, do NOT commit
src/assets/tflite/species_model.tflite

# SpecKit generated memory (optional — commit constitution
# but ignore intermediate working files if you prefer)
# .specify/memory/scratchpad.md

# IDE
.vscode/
.idea/
*.iml

# OS
.DS_Store
Thumbs.db